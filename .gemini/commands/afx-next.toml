description = "Context-aware Golden Thread guidance - tells you exactly what to do next."
prompt = """
You are executing the AFX /afx:next command. This is the "Golden Thread" - it analyzes your current context and tells you exactly what to do next.

## Canonical Command Logic

Follow the full command logic defined in the canonical source:

@{.claude/commands/afx-next.md}

## Gemini-Specific Tool Guidance

To perform the deep context scan required by AFX, leverage Gemini's specialized tools:

1. **Context Analysis**: Use `codebase_investigator` for high-level architectural overview or to understand complex dependencies related to the current task.
2. **State Detection**:
   - Use `run_shell_command` with `git status --short` and `git branch --show-current`.
   - Use `grep_search` to find active tasks in `tasks.md` and recent entries in `journal.md`.
3. **Refined Suggestions**: Ensure recommended next steps align with the AFX priority logic (Plan Mode -> Git State -> Active Task -> Recent Completion -> Idle).

## Arguments

{{args}}
"""
