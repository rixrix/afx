description = "Verify and summarize task implementation status."
prompt = """
You are executing the AFX /afx:task command. This verifies, audits, and manages task implementation status.

## Canonical Command Logic

Follow the full command logic defined in the canonical source:

@{.claude/commands/afx-task.md}

## Gemini-Specific Tool Guidance

When managing tasks:

1. **Verification**: Use `read_file` to compare implementation against task requirements in `tasks.md`.
2. **Audit**: Use `grep_search` to review all tasks for completion criteria.
3. **Closing Tasks**: Use `read_file` to ensure both `[Agent OK]` and `[Human OK]` columns are marked before closing.

## Arguments

{{args}}
"""
